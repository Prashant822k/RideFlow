import { Component, inject, signal } from '@angular/core';
import { User } from '../../models/user.model';
import { UserService } from '../../services/user.service';
@Component({ standalone: true, template: `<section class="heading"><p>YOUR ACCOUNT</p><h1>Profile</h1><span>Your commute preferences and employee details.</span></section>@if (loading()) { <p class="state">Loading profile…</p> } @else { @if (user(); as item) { <section class="profile"><div class="avatar">{{ item.name.charAt(0) }}</div><div><h2>{{ item.name }}</h2><p>{{ item.email }}</p></div></section><section class="info"><h2>Employee information</h2><div><span>Employee ID</span><strong>{{ item.employeeId }}</strong></div><div><span>Preferred pickup location</span><strong>{{ item.preferredPickupLocation }}</strong></div></section> } }`, styles: `.heading p{color:#2563eb;font-weight:800;font-size:.76rem;letter-spacing:.08em;margin:0}.heading h1{color:#17243d;margin:.4rem 0}.heading span{color:#64748b}.profile,.info{margin-top:1.6rem;background:#fff;border:1px solid #e7edf5;border-radius:14px;padding:1.5rem}.profile{display:flex;align-items:center;gap:1rem}.avatar{width:54px;height:54px;border-radius:50%;background:#dbeafe;color:#2563eb;display:grid;place-items:center;font-size:1.4rem;font-weight:800}.profile h2,.info h2{color:#17243d;margin:0}.profile p{color:#64748b;margin:.35rem 0 0}.info div{display:flex;justify-content:space-between;gap:1rem;padding:1rem 0;border-bottom:1px solid #edf1f6;color:#64748b}.info div:last-child{border:0}.info strong{color:#17243d;text-align:right}.state{padding:2rem;background:#fff;border-radius:12px;color:#64748b}` })
export class ProfileComponent { private service = inject(UserService); user = signal<User | undefined>(undefined); loading = signal(true); constructor() { this.service.getUser().subscribe({ next: user => { this.user.set(user); this.loading.set(false); }, error: () => this.loading.set(false) }); } }
