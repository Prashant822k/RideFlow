import { Component, inject, signal } from '@angular/core';
import { RouterLink } from '@angular/router';
import { Ride } from '../../models/ride.model';
import { RideService } from '../../services/ride.service';
import { RideCardComponent } from '../../components/ride-card/ride-card.component';
import { StatusBadgeComponent } from '../../components/status-badge/status-badge.component';
@Component({ standalone: true, imports: [RouterLink, RideCardComponent, StatusBadgeComponent], template: `<section class="intro"><p>MONDAY, 17 AUGUST</p><h1>Good morning, Prashant <span>👋</span></h1><div>Your commute overview</div></section>@if (loading()) { <p class="state">Loading rides…</p> } @else if (error()) { <p class="state error">Unable to load rides. <button (click)="load()">Try again</button></p> } @else { @if (todayRide(); as ride) { <section class="today"><div><p class="eyebrow">TODAY'S RIDE</p><h2>{{ ride.route }}</h2><p class="location">⌖ {{ ride.pickupLocation }}</p><div class="ride-meta"><span>◷ <strong>{{ ride.pickupTime }}</strong></span><span>🚌 {{ ride.vehicle }}</span><span>👤 {{ ride.driver }}</span></div></div><div class="side"><app-status-badge [status]="ride.status" /><a [routerLink]="['/rides', ride.id]">View ride →</a></div></section> } <section class="section-head"><div><h2>Upcoming rides</h2><p>Your next scheduled office commutes.</p></div><a routerLink="/rides">View all →</a></section><div class="grid">@for (ride of upcoming(); track ride.id) { <app-ride-card [ride]="ride" /> }</div> }`, styles: `.intro{margin-bottom:2rem}.intro p,.eyebrow{font-size:.76rem;letter-spacing:.08em;font-weight:800;color:#2563eb;margin:0 0 .5rem}.intro h1{margin:0;color:#17243d;font-size:clamp(1.8rem,4vw,2.45rem)}.intro div{color:#64748b;margin-top:.45rem}.today{background:linear-gradient(135deg,#173b7a,#2563eb);color:#fff;border-radius:18px;padding:1.7rem;display:flex;justify-content:space-between;gap:1.5rem;box-shadow:0 12px 28px #2563eb36}.today .eyebrow{color:#bfdbfe}.today h2{margin:.15rem 0 .55rem;font-size:1.55rem}.location{color:#dbeafe}.ride-meta{display:flex;gap:1rem;flex-wrap:wrap;font-size:.9rem;margin-top:1.35rem;color:#e0ebff}.side{display:flex;flex-direction:column;align-items:flex-end;justify-content:space-between}.side a{color:#fff;text-decoration:none;font-weight:700}.section-head{margin:2.5rem 0 1rem;display:flex;justify-content:space-between;align-items:end}.section-head h2{margin:0;color:#17243d}.section-head p{color:#64748b;margin:.35rem 0 0}.section-head>a{color:#2563eb;text-decoration:none;font-weight:700}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem}.state{padding:2rem;background:#fff;border-radius:12px;color:#64748b}.error{color:#b42318}.error button{border:0;background:none;color:#2563eb;font-weight:700;cursor:pointer}@media(max-width:700px){.today{flex-direction:column}.side{align-items:flex-start;gap:1rem}.grid{grid-template-columns:1fr}.section-head{align-items:start;gap:1rem}}` })
export class DashboardComponent { private service = inject(RideService); rides = signal<Ride[]>([]); loading = signal(true); error = signal(false); todayRide = signal<Ride | undefined>(undefined); upcoming = signal<Ride[]>([]); constructor() { this.load(); } load() { this.loading.set(true); this.error.set(false); this.service.getRides().subscribe({ next: rides => { this.rides.set(rides); this.todayRide.set(rides[0]); this.upcoming.set(rides.slice(1)); this.loading.set(false); }, error: () => { this.error.set(true); this.loading.set(false); } }); } }
